streamlit==1.59.1
openai==2.45.0
python-dotenv==1.2.2
pydantic==2.13.4
langgraph==1.2.9
langsmith==0.10.2
tenacity==9.1.4

# Pinned transitive deps: an unpinned pandas/pyarrow resolved on CI segfaulted
# inside streamlit's dataframe rendering (pandas/core/arrays/string_arrow.py).
# These are the versions verified locally to run the full suite cleanly.
pandas==3.0.3
pyarrow==25.0.0
numpy==2.5.1
